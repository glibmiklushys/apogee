{
  "name": "apogee",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A 2D lunar lander: thrust, rotate, and set down on gold pads.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vite": "^6.2.2",
    "vitest": "^3.0.9"
  }
}
